import React from 'react';
import { WorkDashboard } from '../work-dashboard/work-dashboard';

/** Employee Work tab — wireframe dashboard layout. */
export const WorkTab: React.FC = () => {
  return <WorkDashboard />;
};
